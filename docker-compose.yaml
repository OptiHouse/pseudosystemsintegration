services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=integracje'
      - 'MYSQL_PASSWORD=user'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=user'
    ports:
      - '3306:3306'
